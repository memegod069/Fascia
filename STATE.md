# Fascia — Project State
(Every session reads this first, and updates it before finishing.)

## Current Milestone
M0 — Single-muscle FEM proof (standalone Warp script, no Blender yet)
See docs/MILESTONES.md → M0 for the full acceptance test.

## Last Session Summary
(Nothing yet — this is the first session. Delete this line once real
entries exist, and always keep the most recent session at the top.)

## Known Issues / Open Questions
-

## Exact Next Step
- Install NVIDIA Warp (CPU backend — no GPU on this machine).
- Run Warp's built-in soft-body example unmodified, confirm it executes.
- Report back the output before touching anything else.

## Environment
- Machine: Dell Latitude 7300, 16GB RAM, Intel UHD 620 (no NVIDIA GPU)
- Blender: 4.2 LTS (target — confirm installed version here)
- Python: (fill in)
- Warp: CPU backend (fill in version once installed)
- Cloud burst: Google Colab free tier (T4 GPU) — for M3 onward only
